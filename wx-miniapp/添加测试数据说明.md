# 添加测试数据说明

## 🎯 目标

为小程序添加测试模板数据，让首页能够正常显示模板列表和人群类型分类。

---

## 📋 当前状态

✅ **小程序功能**：已完成，代码正常
✅ **后端服务**：已启动，运行在 http://localhost:3000
✅ **数据库**：已连接，但**没有数据**
❌ **模板数据**：需要添加

---

## 🚀 添加测试数据的方法

### 方法1：使用 MySQL 命令行（推荐）

**第1步：打开 MySQL 命令行**

```bash
mysql -u root -p
```

输入密码：`bademan2025`

**第2步：选择数据库**

```sql
USE ai_travel;
```

**第3步：执行 SQL 脚本**

```bash
source F:/AI拍照玩偶素材内容/软件开发代码/ai_travel_code_20260112/ai-travel-photo-app/scripts/seed-data.sql
```

或者直接复制粘贴 SQL 内容到命令行执行。

**第4步：验证数据**

```sql
SELECT COUNT(*) FROM templates;
SELECT DISTINCT groupType FROM templates;
```

---

### 方法2：使用 MySQL Workbench（图形界面）

1. 打开 MySQL Workbench
2. 连接到数据库：
   - Host: localhost
   - Port: 3306
   - User: root
   - Password: bademan2025
3. 打开 SQL 脚本文件：
   ```
   F:\AI拍照玩偶素材内容\软件开发代码\ai_travel_code_20260112\ai-travel-photo-app\scripts\seed-data.sql
   ```
4. 点击"⚡执行"按钮

---

### 方法3：使用 phpMyAdmin（如果已安装）

1. 访问 http://localhost/phpmyadmin
2. 登录（用户名：root，密码：bademan2025）
3. 选择 `ai_travel` 数据库
4. 点击"SQL"标签
5. 粘贴 SQL 脚本内容
6. 点击"执行"

---

## 📊 测试数据说明

### 包含的数据

- **7 个人群类型**：
  1. 花季少女 (shaonv) - 6 个模板
  2. 熟龄姐姐 (shunv) - 2 个模板
  3. 元气哥哥 (yuanqigege) - 2 个模板
  4. 睿智大叔 (ruizhidashu) - 2 个模板
  5. 软萌女孩 (ruanmengnvhai) - 2 个模板
  6. 儿童 (ertong) - 2 个模板
  7. 长者 (laonian) - 2 个模板

- **共 18 个模板**

### 模板特点

- 所有模板都是免费的（isFree = true）
- 使用占位图片（Picsum 随机图片）
- 覆盖全国各地著名景点
- 符合瀑布流布局（奇数 sortOrder 在左列，偶数在右列）

---

## ✅ 完成后的效果

添加数据后，在微信开发者工具中：

1. **刷新小程序**
   - 按 `Ctrl + B` 重新编译

2. **应该能看到**：
   - ✅ 顶部显示 7 个人群类型标签（可横向滚动）
   - ✅ 默认选中"花季少女"
   - ✅ 模板列表以瀑布流形式显示
   - ✅ 左右两列各显示 3 个模板
   - ✅ 点击不同人群类型可以切换模板

3. **测试功能**：
   - 点击顶部标签切换人群类型
   - 下拉刷新模板列表
   - 点击任意模板进入详情页

---

## 🔧 如果还是显示"暂无模板"

### 检查清单：

1. **确认后端正在运行**
   ```
   命令行显示：Server running on http://localhost:3000
   ```

2. **确认数据已添加**
   ```sql
   USE ai_travel;
   SELECT COUNT(*) FROM templates;
   -- 应该显示 18
   ```

3. **查看 Console 错误**
   - 微信开发者工具 → Console 标签
   - 查看是否有红色错误信息

4. **重新编译小程序**
   - 按 `Ctrl + B`
   - 或点击"编译"按钮

---

## 📞 需要帮助？

如果遇到问题，请提供：
1. MySQL 执行结果的截图
2. 小程序 Console 的截图
3. 具体的错误信息

---

**祝你成功！** 🎉
